import Vue from 'vue'
import {
  Icon,
  Button,
  Layout,
  BackTop,
  PageHeader,
  Tag,
  Divider,
  Timeline,
  Card,
  List,
  Avatar,
  Affix,
  Row,
  Col,
  Input,
  Collapse
} from "ant-design-vue";
Vue.use(Icon).use(Button).use(Layout).use(BackTop).use(PageHeader).use(Tag).use(Divider).use(Timeline).use(Card).use(List)
  .use(Avatar).use(Affix).use(Row).use(Col).use(Input).use(Collapse)
